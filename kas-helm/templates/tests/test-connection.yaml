apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kas-helm.fullname" . }}-test-connection"
  labels:
    {{- include "kas-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kas-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
