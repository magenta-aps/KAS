apiVersion: v1
kind: Service
metadata:
  name: {{ include "kas-helm.fullname" . }}
  labels:
    {{- include "kas-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kas-helm.selectorLabels" . | nindent 4 }}
